

<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
  {% for card in cards %}
    <div class="bg-white rounded-xl shadow p-5 border border-gray-100">
      <div class="mb-2">
        <a href="{{ card.repo_url }}" target="_blank" class="text-lg font-semibold text-indigo-600 hover:underline">
          {{ card.repo_name }}
        </a>
        <p class="text-sm text-gray-600 mt-1">{{ card.description }}</p>
      </div>

      <div class="mt-3">
        <a href="{{ card.issue_url }}" target="_blank" class="block font-medium text-gray-800 hover:underline">
          🐛 {{ card.issue_title }}
        </a>
        <div class="flex flex-wrap mt-2 gap-1">
          {% for label in card.labels %}
            <span class="bg-gray-100 text-xs px-2 py-1 rounded-full text-gray-700">{{ label }}</span>
          {% endfor %}
        </div>
      </div>

      <div class="mt-4 text-sm text-gray-500 flex justify-between items-center">
        <span>🌐 {{ card.language }}</span>
        <span>⭐ {{ card.stars }}</span>
      </div>
    </div>
  {% empty %}
    <p>No issues found.</p>
  {% endfor %}
</div>